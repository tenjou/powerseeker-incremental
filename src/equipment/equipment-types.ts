import { EquipmentType, EquipmentSlotType } from "../config/item-configs"

export const EquipmentSlot: Record<EquipmentType, EquipmentSlotType> = {
    armor: "body",
    axe: "main_hand",
    shield: "off_hand",
    sword: "main_hand",
}
